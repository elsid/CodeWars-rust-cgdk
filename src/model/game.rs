#[derive(Clone, Debug, PartialEq)]
pub struct Game {
    pub random_seed: i64,
    pub tick_count: i32,
    pub world_width: f64,
    pub world_height: f64,
    pub fog_of_war_enabled: bool,
    pub victory_score: i32,
    pub facility_capture_score: i32,
    pub vehicle_elimination_score: i32,
    pub action_detection_interval: i32,
    pub base_action_count: i32,
    pub additional_action_count_per_control_center: i32,
    pub max_unit_group: i32,
    pub terrain_weather_map_column_count: i32,
    pub terrain_weather_map_row_count: i32,
    pub plain_terrain_vision_factor: f64,
    pub plain_terrain_stealth_factor: f64,
    pub plain_terrain_speed_factor: f64,
    pub swamp_terrain_vision_factor: f64,
    pub swamp_terrain_stealth_factor: f64,
    pub swamp_terrain_speed_factor: f64,
    pub forest_terrain_vision_factor: f64,
    pub forest_terrain_stealth_factor: f64,
    pub forest_terrain_speed_factor: f64,
    pub clear_weather_vision_factor: f64,
    pub clear_weather_stealth_factor: f64,
    pub clear_weather_speed_factor: f64,
    pub cloud_weather_vision_factor: f64,
    pub cloud_weather_stealth_factor: f64,
    pub cloud_weather_speed_factor: f64,
    pub rain_weather_vision_factor: f64,
    pub rain_weather_stealth_factor: f64,
    pub rain_weather_speed_factor: f64,
    pub vehicle_radius: f64,
    pub tank_durability: i32,
    pub tank_speed: f64,
    pub tank_vision_range: f64,
    pub tank_ground_attack_range: f64,
    pub tank_aerial_attack_range: f64,
    pub tank_ground_damage: i32,
    pub tank_aerial_damage: i32,
    pub tank_ground_defence: i32,
    pub tank_aerial_defence: i32,
    pub tank_attack_cooldown_ticks: i32,
    pub tank_production_cost: i32,
    pub ifv_durability: i32,
    pub ifv_speed: f64,
    pub ifv_vision_range: f64,
    pub ifv_ground_attack_range: f64,
    pub ifv_aerial_attack_range: f64,
    pub ifv_ground_damage: i32,
    pub ifv_aerial_damage: i32,
    pub ifv_ground_defence: i32,
    pub ifv_aerial_defence: i32,
    pub ifv_attack_cooldown_ticks: i32,
    pub ifv_production_cost: i32,
    pub arrv_durability: i32,
    pub arrv_speed: f64,
    pub arrv_vision_range: f64,
    pub arrv_ground_defence: i32,
    pub arrv_aerial_defence: i32,
    pub arrv_production_cost: i32,
    pub arrv_repair_range: f64,
    pub arrv_repair_speed: f64,
    pub helicopter_durability: i32,
    pub helicopter_speed: f64,
    pub helicopter_vision_range: f64,
    pub helicopter_ground_attack_range: f64,
    pub helicopter_aerial_attack_range: f64,
    pub helicopter_ground_damage: i32,
    pub helicopter_aerial_damage: i32,
    pub helicopter_ground_defence: i32,
    pub helicopter_aerial_defence: i32,
    pub helicopter_attack_cooldown_ticks: i32,
    pub helicopter_production_cost: i32,
    pub fighter_durability: i32,
    pub fighter_speed: f64,
    pub fighter_vision_range: f64,
    pub fighter_ground_attack_range: f64,
    pub fighter_aerial_attack_range: f64,
    pub fighter_ground_damage: i32,
    pub fighter_aerial_damage: i32,
    pub fighter_ground_defence: i32,
    pub fighter_aerial_defence: i32,
    pub fighter_attack_cooldown_ticks: i32,
    pub fighter_production_cost: i32,
    pub max_facility_capture_points: f64,
    pub facility_capture_points_per_vehicle_per_tick: f64,
    pub facility_width: f64,
    pub facility_height: f64,
    pub base_tactical_nuclear_strike_cooldown: i32,
    pub tactical_nuclear_strike_cooldown_decrease_per_control_center: i32,
    pub max_tactical_nuclear_strike_damage: f64,
    pub tactical_nuclear_strike_radius: f64,
    pub tactical_nuclear_strike_delay: i32,
}
